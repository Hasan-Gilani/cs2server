# ─────────────────────────────────────────────────────────────────────────────
# CS2 Dedicated Server — Environment Configuration
# Copy this file to .env and fill in your values.
# ─────────────────────────────────────────────────────────────────────────────

# ── Paths ──────────────────────────────────────────────────────────────────────
# Where SteamCMD will be installed
STEAMCMD_DIR=$HOME/steamcmd

# Where CS2 server files will be installed
CS2_DIR=$HOME/cs2

# ── Server Identity ────────────────────────────────────────────────────────────
SERVER_NAME="My CS2 Server"

# ── Game Settings ──────────────────────────────────────────────────────────────
# Starting map
MAP=de_dust2

# Max player slots
MAX_PLAYERS=10

# Game type / mode:
#   GAME_TYPE=0, GAME_MODE=0  → Casual
#   GAME_TYPE=0, GAME_MODE=1  → Competitive
#   GAME_TYPE=0, GAME_MODE=2  → Wingman
#   GAME_TYPE=1, GAME_MODE=0  → Arms Race (Gungame)
#   GAME_TYPE=1, GAME_MODE=1  → Demolition
#   GAME_TYPE=1, GAME_MODE=2  → Deathmatch
GAME_TYPE=0
GAME_MODE=1

# Server tickrate (64 or 128)
TICKRATE=128

# ── Network ────────────────────────────────────────────────────────────────────
PORT=27015
TV_PORT=27020
CLIENT_PORT=27005

# ── Security ───────────────────────────────────────────────────────────────────
# Leave SERVER_PASSWORD blank for a public server
SERVER_PASSWORD=your_server_password

# RCON (remote console) password — change this!
RCON_PASSWORD=your_rcon_password

# Steam Game Server Login Token (GSLT)
# Required for internet-visible servers. Get one at:
# https://steamcommunity.com/dev/managegameservers (App ID 730)
STEAM_ACCOUNT=your_gslt_token

# ── Launch Options ─────────────────────────────────────────────────────────────
# Set to "true" to run inside a screen session (lets you detach)
USE_SCREEN=false
SCREEN_NAME=cs2server

# ── Discord Notifications ───────────────────────────────────────────────────────
# Webhook URL from: Discord channel → Edit → Integrations → Webhooks → New Webhook → Copy URL
# Leave blank to disable startup notifications.
DISCORD_WEBHOOK_URL=
